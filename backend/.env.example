# Exemplo de configuração de conexão com o MySQL (Railway)
# Defina a variável DATABASE_URL para uso pelo Prisma
# Exemplo (substitua pelos valores reais da Railway):
# DATABASE_URL="mysql://root:senha@host:3306/railway"

DATABASE_URL="${MYSQL_URL}"

PORT=4000
